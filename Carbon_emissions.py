API_KEY = os.environ["API_KEY_HERE"]

# Step 1: Read in data
df = read.csv('')

# Step 2: Create data pipeline
def data_pipeline(dataframe)
  return df_validated

# Step 3: Get Updated dataframe with carbon footprint after API query
def carbon_footprint(dataframe):
  return df_updated

# Step 4: Put everything on Streamlit
if "history" not in st.session_state:
    st.session_state.history = []

st.header("")
st.image(
    "image link",
    caption="caption",
    use_container_width=True
)
